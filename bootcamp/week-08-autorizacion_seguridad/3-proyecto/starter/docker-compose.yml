name: bc-week08-proyecto

services:
  mongo:
    image: mongo:7
    container_name: bc-week08-proyecto-mongo
    restart: unless-stopped
    ports:
      - "27021:27017"
    volumes:
      - proyecto-mongo-data:/data/db

volumes:
  proyecto-mongo-data:
